kind: pipeline
name: backend

steps:
- name: test
  image: node:latest
  commands:
  - echo test
  - npm install
  - npm test:unit
  when:
    branch:
      - master
    event:
      - push
      - pull-request